/** @type {import('next').NextConfig} */
const nextConfig = {
    /* experimental: {
        appDir: true,
    }, */
    reactStrictMode: false, // React Strict Mode is off
};

module.exports = nextConfig;
